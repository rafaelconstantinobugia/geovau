# App Configuration
NEXT_PUBLIC_APP_NAME="Vau Explorer"
NEXT_PUBLIC_BRAND_PRIMARY="#FF6A00"
NEXT_PUBLIC_BRAND_BG="#000000"
NEXT_PUBLIC_DEMO_ENABLED="true"
NEXT_PUBLIC_ADMIN_ENABLED="true"

# Mapbox (public token - safe to expose)
NEXT_PUBLIC_MAPBOX_TOKEN="pk.****************"  # Get from https://mapbox.com/

# Supabase (client read-only; writes via Edge Function only)
NEXT_PUBLIC_SUPABASE_URL="https://XXXX.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOi..."      # anon key (public by design)

# Supabase Edge Functions (server-side only)
SUPABASE_SERVICE_ROLE_KEY="****************"       # server only - for Edge Functions
ALLOW_ORIGIN="https://vau.lovable.app"             # CORS allowed origin

# Optional integrations (not used in MVP)
RESEND_API_KEY=""                                   # Email service (future use)
STRIPE_SECRET_KEY=""                                # Payments (future use)

# Usage Notes:
# - NEXT_PUBLIC_* variables are exposed to the client browser
# - MAPBOX_TOKEN must be a public token from Mapbox dashboard
# - SUPABASE_SERVICE_ROLE_KEY is for server-side operations only
# - Get Supabase keys from: Project Settings > API
# - Enable CORS for your domain in Supabase dashboard